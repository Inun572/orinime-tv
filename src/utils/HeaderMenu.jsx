const HeaderMenu = ({ title }) => {
  return (
    <div className="header-menu text-color-primary font-bold text-2xl text-center gap-4">
      {title}
    </div>
  );
};

export default HeaderMenu;
